#include <iostream>
#include <fstream>
#include <map>
#include <list>
#include <string>

using namespace std;

#ifndef IO_FILE
#define IO_FILE

string addFile(string text, string fileName);
int searchFile(string fileName, string text);
#endif
